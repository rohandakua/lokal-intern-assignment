package com.example.lokalinternassignment.domain.constantsDemo

val baseURL = "API"

/**
 * create a directory in domain named as "constants"
 * create a file name baseURL.kt and make a variable names as "baseURL" of type String and place your api
 * in that variable like
 * baseURL = "demo.demo.demo"
 *
 * make this import in the Retrofit Builder obj in the koinModule in di directory
 */